# 本机操作

适用于操作系统、文件系统、本地应用、磁盘空间、清理和维护任务。

先用范围明确的只读检查定位实际对象。能够检查指定目录、文件或应用时，不扫描整个磁盘或无关位置。

普通只读检查和已获授权的可逆本地操作可直接继续。删除、覆盖、批量移动或修改系统关键位置前，核对解析后的绝对路径和实际影响；用户没有授权时先询问。

不要把临时诊断文件散落在工作区根目录；需要保留的排障结论放入 `Chats\Troubleshooting`，临时输出放入 `Temp`。完成后立即复查目标状态，不把未经验证的尝试报告为成功。
